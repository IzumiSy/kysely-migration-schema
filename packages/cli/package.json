{
  "name": "@izumisy/kysely-migration-schema",
  "private": false,
  "type": "module",
  "version": "0.1.0",
  "description": "Declative schema manager for kysely migration",
  "files": [
    "dist"
  ],
  "bin": {
    "kysely-migration-schema": "./dist/main.js"
  },
  "scripts": {
    "dev": "NODE_ENV=development tsup --watch",
    "build": "tsup",
    "type-check": "tsc --noEmit --incremental --pretty"
  },
  "dependencies": {
    "c12": "^3.1.0",
    "citty": "^0.1.6",
    "kysely": "^0.28.3",
    "kysely-codegen": "^0.18.5",
    "zod": "^4.0.14"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "prettier": "^3.6.2",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  }
}
